Aplicativo React + Vite
Este é um guia simples sobre como executar o aplicativo React + Vite localmente em sua máquina.

Pré-requisitos
Certifique-se de ter o Node.js e o npm (gerenciador de pacotes Node.js) instalados em sua máquina.

Passos para Executar
Clone o repositório:

bash
Copy code
git clone https://github.com/leofnh/reactteste
Navegue até o diretório do aplicativo:

bash
Copy code
cd seu-app-react
Instale as dependências:

bash
Copy code
npm install
Inicie o servidor de desenvolvimento:

bash
Copy code
npm run dev
Acesse o aplicativo em seu navegador:

Abra o navegador e vá para http://localhost:3000

Como Parar o Servidor
Para parar o servidor de desenvolvimento, pressione Ctrl + C no terminal onde o servidor está sendo executado.